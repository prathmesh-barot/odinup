name: Build and Release OdinUP

on:
  push:
    tags:
      - 'v*'

jobs:
  build:
    name: Build ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false # Prevents the whole pipeline from stopping if 1 target fails!
      matrix:
        include:
          - os: ubuntu-latest
            target: linux_amd64
            name: odinup-linux-amd64
            exe: odinup
            setup_cmd: ""
            
          - os: ubuntu-latest
            target: linux_arm64
            name: odinup-linux-arm64
            exe: odinup
            setup_cmd: "sudo apt-get update && sudo apt-get install -y gcc-aarch64-linux-gnu lld clang"
            
          - os: macos-latest
            target: darwin_amd64
            name: odinup-darwin-amd64
            exe: odinup
            setup_cmd: ""
            
          - os: macos-latest
            target: darwin_arm64
            name: odinup-darwin-arm64
            exe: odinup
            setup_cmd: ""
            
          - os: windows-latest
            target: windows_amd64
            name: odinup-windows-amd64
            exe: odinup.exe
            setup_cmd: ""
            
          - os: windows-latest
            target: windows_i386
            name: odinup-windows-i386
            exe: odinup.exe
            setup_cmd: ""

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Install Odin Compiler
        uses: laytan/setup-odin@v2
        with:
          release: "latest" # Fixed the warning from your screenshot

      - name: Install Cross-Linker
        if: matrix.setup_cmd != ''
        run: ${{ matrix.setup_cmd }}

      - name: Compile OdinUP
        run: odin build . -target:${{ matrix.target }} -out:${{ matrix.exe }} -o:speed

      - name: Package Archive (Tar.gz)
        shell: bash
        run: tar -czf ${{ matrix.name }}.tar.gz ${{ matrix.exe }}

      - name: Upload Artifact to Workflow
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.name }}
          path: ${{ matrix.name }}.tar.gz

  release:
    name: Publish GitHub Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Download all Compiled Archives
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create Release & Attach Binaries
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/*.tar.gz
          generate_release_notes: true